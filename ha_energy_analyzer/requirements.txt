# Core requirements for Home Assistant Add-on (lightweight)
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
python-dateutil>=2.8.0
pytz>=2023.3

# Emporia Vue integration
pyemvue>=0.15.0

# Note: matplotlib removed to avoid complex C++ build dependencies
# Charts can be generated in the main project if needed